import { Button } from "@/shared/ui/button";

type ModalWindowProps = object

export const ModalWindow = ({}: ModalWindowProps) => {
    return (
        <div>
            <Button/>
        </div>
    )
}
